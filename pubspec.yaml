name: ar_flutter
description: A new Flutter project.
publish_to: 'none'
version: 0.0.1+1
environment:
  sdk: ">=2.8.0 <3.0.0"
dependencies:
  arcore_flutter_plugin: ^0.0.11
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true